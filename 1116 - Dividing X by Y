#include<iostream>
using namespace std;
int main()
{
int n,a,b,i;
cin>>n;
for(i=0;i<n;i++)
{
    cin>>a;
    cin>>b;
    if(b==0)
    {
        cout<<"divisao impossivel"<<endl;
    }
    else
    {
     printf("%.1f\n",(float)a/(float)b);
    }
}
return 0;
}
